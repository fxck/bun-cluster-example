{
  "name": "bun-cluster-example",
  "version": "0.0.0",
  "scripts": {
    "start": "bun dist/cluster.js",
    "build": "bun build cluster.ts --outfile=dist/cluster.js",
    "dev": "bun run cluster.ts"
  },
  "devDependencies": {
    "bun-types": "latest",
    "@types/node": "latest"
  }
}
