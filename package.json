{
  "name": "bun-cluster-example",
  "version": "0.0.0",
  "scripts": {
    "start": "bun dist/cluster.js",
    "build": "bun build.ts",
    "dev": "bun run cluster.ts"
  },
  "devDependencies": {
    "bun-types": "latest",
    "@types/node": "latest"
  }
}
