{
  "name": "bun-pm2-example",
  "version": "0.0.0",
  "scripts": {
    "start": "pm2 start ecosystem.config.js --no-daemon",
    "build": "bun build.ts",
    "dev": "bun run app.ts"
  },
  "dependencies": {
    "pm2": "5.4.3"
  },
  "devDependencies": {
    "bun-types": "latest"
  }
}
